# for core data processing
pandas >= 2.0.0
numpy >= 1.24.0
pyarrow >= 14.0.0   # for Parquet read/write

# for data ingestion
requests >= 2.31.0  # is for HTTP downloads and API calls
click>=8.1.7

# ERA5 access (register free at cds.climate.copernicus.eu)
cdsapi>=0.6.1         # Copernicus Climate Data Store API client
psycopg2-binary>=2.9.10
SQLAlchemy>=2.0.36
netCDF4>=1.6.5        # NetCDF file reading for ERA5
xarray>=2023.12.0     # Label-aware arrays for NetCDF/ERA5 data

# Analysis and ML (preview for M4)
scikit-learn>=1.3.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Spark (ensure Java 11+ is installed separately)
pyspark>=3.5.0

# Workflow
apache-airflow>=2.9.0
